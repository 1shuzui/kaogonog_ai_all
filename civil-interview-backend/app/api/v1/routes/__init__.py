"""
v1 路由包入口。

这个目录只做 HTTP 适配：校验请求、注入当前用户和数据库会话、调用 service、返回兼容前端的数据结构。把业务判断留在 services/core 层，是为了让 PC、小程序和维护脚本复用同一套规则。

@param: 无；这是包初始化文件，不接收业务请求。
@return: 无直接返回；提供各路由模块的统一导入边界。
@raises ImportError: Python 包路径或依赖环境异常时会在导入阶段失败。
"""
