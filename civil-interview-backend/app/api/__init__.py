"""
HTTP API 包入口。

这里承载接口版本目录，不直接声明业务路由。这样做是为了让 `/api/v1` 之后还能并存新版本接口，同时保持旧端继续按原路径访问，避免 PC 和小程序发布节奏不同导致接口被动破坏。

@param: 无；这是包初始化文件，不接收业务请求。
@return: 无直接返回；提供 API 分层导入路径。
@raises ImportError: Python 包路径或依赖环境异常时会在导入阶段失败。
"""
