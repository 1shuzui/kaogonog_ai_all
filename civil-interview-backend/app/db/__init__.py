"""
数据库访问包入口。

数据库会话、引擎和连接参数都收在 `app.db.session`，避免服务层自己创建连接。这个入口保持轻量，是为了让脚本、测试和 FastAPI 依赖注入都能复用同一个 MySQL 会话边界。

@param: 无；这是包初始化文件，不接收业务请求。
@return: 无直接返回；提供数据库模块的稳定导入路径。
@raises ImportError: Python 包路径或依赖环境异常时会在导入阶段失败。
"""
