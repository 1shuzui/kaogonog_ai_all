"""
后端核心基础设施包入口。

`core` 放配置、鉴权、AI 网关、缓存和访问权限这些跨业务模块能力。它们被多条接口链路复用，所以包入口保持空实现，避免导入 core 时隐式初始化模型、Redis 或数据库连接。

@param: 无；这是包初始化文件，不接收业务请求。
@return: 无直接返回；提供核心能力模块的稳定导入路径。
@raises ImportError: Python 包路径或依赖环境异常时会在导入阶段失败。
"""
