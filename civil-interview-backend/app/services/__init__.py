"""
业务服务包入口。

services 承担题库、考试、评分、订阅、支付、反馈和管理员操作的业务编排。路由层只做 HTTP 适配，真正的校验、事务和兼容规则应放在这里，方便 PC、小程序、脚本和测试共享同一套行为。

@param: 无；这是包初始化文件，不接收业务请求。
@return: 无直接返回；提供服务模块的稳定导入路径。
@raises ImportError: Python 包路径或业务依赖异常时会在导入阶段失败。
"""
