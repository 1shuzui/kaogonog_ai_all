"""
后端应用包入口。

`app` 包按“路由适配、服务编排、模型定义、配置基础设施”分层组织，FastAPI 启动文件和测试都从这里向下导入。这个文件不放业务逻辑，是为了避免导入包时顺带建表、连库或注册路由，降低脚本和测试的副作用。

@param: 无；这是包初始化文件，不接收业务请求。
@return: 无直接返回；提供稳定包路径给 main.py、测试和维护脚本使用。
@raises ImportError: Python 包路径或依赖环境异常时会在导入阶段失败。
"""
