# 2026-09-19 功能验收修复记录

依据 Windows 验收清单逐项核对；原文件保持不变，未测试项不视为通过。

## 第一批：登录会话与账号隔离

- 用户确认规则：电脑与微信可同时使用，同类客户端的新登录替换旧会话；错误密码不撤销有效会话。
- 新增 `user_login_sessions` 表保存两类会话标识；由现有启动建表流程补齐。旧 token 在首次新登录前继续有效。
- `/token` 增加可选 `client_type=web|wechat`，默认 web；小程序密码登录和微信快捷登录均使用 wechat。
- 登录失败仅显示中文原因，不作为会话过期跳转；受保护请求的 401 清空账号状态并引导重新登录。
- 账号切换清理内存中的考试、历史、训练、权益和收藏状态；旧 token 发出的迟到请求不写入新账号页面。
- 训练进度、权益和偏好缓存按账号区分；不认领来源不明的旧共享缓存，不删除旧缓存文件。
- 管理员权限、支付核验和外部模型评分配置保持原样，`LOCAL_REFERENCE_SCORING=false`。

验证：后端 106 项通过；两端 Node 逻辑测试 14 项通过。测试用短 JWT 密钥有警告，不是生产配置。客户端生产构建另行验证；以上不等于微信真机验收。

## 第二批：练习与成绩契约

- 年份勾选即时检索；随机题继承列表全部筛选条件，准备页使用真实 questionId，不二次随机换题。
- 专项训练可选择 1 / 3 / 5 题，支持连续练习或单独选题；不足请求题量时只展示实际题目，不复制凑题。
- 两端加入文字作答；录音、录像与原文重试保留既有流程。PC 后台评分失败记录在答案状态中，避免未处理的异步异常。
- 开考保存练习类型；旧数据库迁移可重复执行，历史旧记录不猜测类型。
- 题目赋分、能力百分制、整套仪态分分开计算；历史总评、个人统计与趋势共用聚合逻辑，不把 36 分制直接当百分制，也不只保留最后一题的能力表现。
- 小程序补充录像回放、套卷数量提示和个人能力总结。

验证：后端 114 项通过，两端 Node 17 项通过；新增测试覆盖筛选一致性、空结果不放宽条件、部分提交、跨账号不可读、旧库升级幂等、不同满分与套题仪态分、文字评分不调用 ASR、模型失败后保留原文重试、找回密码拆页后保留微信隐私/协议回调。

复核命令（仓库根目录，使用已安装的 Node 24 与 Python 虚拟环境）：

```bash
(cd civil-interview-backend && ../.venv/bin/python -m pytest -q)
node --experimental-vm-modules --test civil-interview-frontend/tests/*.test.mjs civil-interview-miniprogram/tests/*.test.mjs
.venv/bin/python scripts/validate_project_docs.py
(cd civil-interview-frontend && npm run build)
(cd civil-interview-miniprogram && npm run build:mp-weixin:prod)
```

两端生产构建通过，小程序 37 个 JSON 配置通过解析。Node 测试需 VM 模块开关；忘记此开关会报测试运行器错误，不代表产品运行失败。测试环境存在短 JWT 密钥警告、VM 模块实验警告，PC 仍有既有的图表/组件库大分块提示；本次未新增运行时依赖，也未将这些警告写成无警告通过。

## 第三批：账号界面

- 两端独立找回密码页面；验证码仍由管理员核验后发送，不承诺自动短信或邮件。核验与改密由后端单次确认完成。
- 注册提示统一 3–32 位字母、数字、下划线或短横线。微信快捷账号设置页提供已有电脑账号的登录入口，不自动合并两个账号的数据。
- 小程序增加昵称、邮箱编辑，个人页明确资料入口；换号补齐订阅、定向与题库内存状态清理。

## 服务器与页面检查

- SSH 读取、健康接口与独立私有测试文件的创建/读取/修改/清理均成功；未修改真实用户答案、订单或管理员权限。
- 当前服务器题库有 366 套完整套卷：山东 155、江苏 161、安徽 40、湖南 10，均至少 2 题。验收表“只有一套”不能据此判断题库缺失，界面补充套数与显式选卷提示。
- 本地 Browser 正文读取和点击跳转找回密码页成功；截图曾返回空白，不能据此宣称视觉验收通过。
- 微信真机录音录像、正式支付与原清单未测试部分需单独验收；编译通过不替代真机发布和设备权限测试。
