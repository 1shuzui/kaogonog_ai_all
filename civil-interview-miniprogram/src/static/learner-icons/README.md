# 考生端本地图标

来源：https://github.com/ant-design/ant-design-icons ，MIT，授权原文见 LICENSE.txt。
从 PC 端现有的 `@ant-design/icons-svg` 依赖中精选 16 个 outlined 图标，
仅将默认填充色统一为 `#326BE5`，未改动原始几何路径。

重新生成：在仓库根目录执行 `node scripts/export_learner_icons.mjs`。
全部随小程序包发布，不在运行时请求图标或字体 CDN。
